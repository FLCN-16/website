import styled from 'styled-components';

import LogoImg from '../../../public/assets/logo.svg';

export const HeaderLogo = styled(LogoImg)`
  max-height: 100%;
  margin-left: auto;
  color: #000000;
  filter: drop-shadow(rgba(0, 0, 0, 0.15) 5px 5px 1px);
`;
