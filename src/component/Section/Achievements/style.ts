import styled from 'styled-components';
import { motion } from 'framer-motion';


export const AchievementSliderSlide = styled(motion.div).attrs({
  className: 'flex flex-col items-center py-16 px-8',
})``;